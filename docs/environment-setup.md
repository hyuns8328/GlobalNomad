# 🛠 Environment Setup

본 프로젝트는 팀 전체가 동일한 코드 품질을 유지할 수 있도록  
ESLint, Prettier, Husky, lint-staged 기반으로 환경을 설정했습니다.

아래 내용을 따라하면 동일한 개발 환경을 구성할 수 있습니다.

---

## 📦 Install

다음 개발용 패키지가 설치되어 있습니다:

```bash
pnpm add -D \
  eslint \
  @typescript-eslint/parser \
  @typescript-eslint/eslint-plugin \
  prettier \
  eslint-config-prettier \
  eslint-plugin-prettier \
  husky \
  lint-staged
```
